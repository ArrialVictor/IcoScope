[pytest]
# Qt signal delivery on macOS conflicts with pytest's stdout capture and
# crashes inside QCoreApplication::processEvents. Disable capture for
# every GUI test — these tests are slow and noisy by nature anyway, so
# losing the capture isn't a real cost.
addopts = -s
